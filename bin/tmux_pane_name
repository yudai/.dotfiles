#!/bin/bash

pane_title=$1
pane_index=$2

nice_pane_title=""
nice_pane_index="${pane_index}"

if [ "${pane_title:0:1}" != "[" ]; then
    nice_pane_title="${pane_title}@"
fi

echo -n "${nice_pane_title}${nice_pane_index}"
